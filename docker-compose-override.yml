services:
    app:
        build:
            context: .
            dockerfile: .docker/php/Dockerfile-dev

        volumes:
            - ./:/app
            - ./var:/app/var

        environment:
            APP_ENV: dev

        extra_hosts:
            - "host.docker.internal:host-gateway"

        command: tail -f /dev/null
        stdin_open: true
        tty: true